# Auto-generated by setup.sh — 4 equipe(s)
# Toggle "0" / "1" pour activer le branding Nantes@Hack sur tous les services.
x-build-args: &build-args
  VITE_NANTES_HACK: "1"

services:
  # Central live dashboard (to project on screen)
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile.dashboard
      args: *build-args
    ports:
      - "5000:5000"
    restart: unless-stopped

  # ──────────────────────── Team 1 ────────────────────────
  site-team1:
    build:
      context: .
      dockerfile: Dockerfile.site
      args: *build-args
    environment:
      - TEAM_NAME=Alpha
      - DASHBOARD_URL=http://dashboard:5000
    ports:
      - "3001:3000"
    depends_on:
      - dashboard
    restart: unless-stopped

  exploit-team1:
    build:
      context: .
      dockerfile: Dockerfile.exploit
      args: *build-args
    environment:
      - TEAM_NAME=Alpha
      - TEAM_PASSWORD=aCzSP
      - SITE_URL=http://site-team1:3000
      - DASHBOARD_URL=http://dashboard:5000
    ports:
      - "4001:4000"
    restart: unless-stopped

  # ──────────────────────── Team 2 ────────────────────────
  site-team2:
    build:
      context: .
      dockerfile: Dockerfile.site
      args: *build-args
    environment:
      - TEAM_NAME=Bravo
      - DASHBOARD_URL=http://dashboard:5000
    ports:
      - "3002:3000"
    depends_on:
      - dashboard
    restart: unless-stopped

  exploit-team2:
    build:
      context: .
      dockerfile: Dockerfile.exploit
      args: *build-args
    environment:
      - TEAM_NAME=Bravo
      - TEAM_PASSWORD=qGZE6
      - SITE_URL=http://site-team2:3000
      - DASHBOARD_URL=http://dashboard:5000
    ports:
      - "4002:4000"
    restart: unless-stopped

  # ──────────────────────── Team 3 ────────────────────────
  site-team3:
    build:
      context: .
      dockerfile: Dockerfile.site
      args: *build-args
    environment:
      - TEAM_NAME=Charlie
      - DASHBOARD_URL=http://dashboard:5000
    ports:
      - "3003:3000"
    depends_on:
      - dashboard
    restart: unless-stopped

  exploit-team3:
    build:
      context: .
      dockerfile: Dockerfile.exploit
      args: *build-args
    environment:
      - TEAM_NAME=Charlie
      - TEAM_PASSWORD=XL2Xe
      - SITE_URL=http://site-team3:3000
      - DASHBOARD_URL=http://dashboard:5000
    ports:
      - "4003:4000"
    restart: unless-stopped

  # ──────────────────────── Team 4 ────────────────────────
  site-team4:
    build:
      context: .
      dockerfile: Dockerfile.site
      args: *build-args
    environment:
      - TEAM_NAME=Delta
      - DASHBOARD_URL=http://dashboard:5000
    ports:
      - "3004:3000"
    depends_on:
      - dashboard
    restart: unless-stopped

  exploit-team4:
    build:
      context: .
      dockerfile: Dockerfile.exploit
      args: *build-args
    environment:
      - TEAM_NAME=Delta
      - TEAM_PASSWORD=60pGY
      - SITE_URL=http://site-team4:3000
      - DASHBOARD_URL=http://dashboard:5000
    ports:
      - "4004:4000"
    restart: unless-stopped
